# name: VITA Backend CI/CD

# on:
#   push:
#     branches: [main, stg]
#   pull_request:
#     branches: [main, stg]

# permissions:
#   id-token: write
#   contents: read

# env:
#   AWS_REGION: us-east-1
#   PYTHON_VERSION: "3.12"

# # =========================
# # JOBS
# # =========================

# jobs:
#   # -------------------------
#   # LINT / FORMAT
#   # -------------------------
#   lint:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
#           cache: "pip"

#       - name: Install Ruff
#         run: pip install ruff==0.15.12

#       - name: Ruff check
#         run: ruff check . --output-format=github

#       - name: Ruff format check
#         run: ruff format . --check

#   # -------------------------
#   # UNIT TESTS
#   # -------------------------
#   unit-tests:
#     runs-on: ubuntu-latest

#     env:
#       POSTGRES_USER: test
#       POSTGRES_PASSWORD: test
#       POSTGRES_SERVER: localhost
#       POSTGRES_PORT: 5432
#       POSTGRES_DB: test_db
#       JWT_SECRET: test-secret
#       JWT_ALGORITHM: HS256
#       JWT_EXPIRATION_TIME: 3600

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
#           cache: "pip"

#       - name: Install dependencies
#         run: |
#           pip install -r requirements.txt
#           pip install pytest pytest-asyncio pytest-cov

#       - name: Run unit tests
#         run: |
#           pytest tests -m "unit" --tb=short

#   # -------------------------
#   # INTEGRATION TESTS (Testcontainers)
#   # -------------------------
#   integration-tests:
#     runs-on: ubuntu-latest

#     env:
#       POSTGRES_USER: test
#       POSTGRES_PASSWORD: test
#       POSTGRES_SERVER: localhost
#       POSTGRES_PORT: 5432
#       POSTGRES_DB: test_db
#       JWT_SECRET: test-secret
#       TESTCONTAINERS_HOST_OVERRIDE: localhost
#       TESTCONTAINERS_CHECKS_DISABLE: true
#       POSTGRES_IMAGE: postgres:15-alpine
#       TRATO_API_KEY: trato_test_aaaa0BBaBaBBaa000aBaB0

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
#           cache: "pip"
#           cache-dependency-path: requirements.txt

#       - name: Install deps
#         run: |
#           pip install -r requirements.txt
#           pip install pytest pytest-asyncio pytest-cov testcontainers pytest-xdist

#       - name: Verify Docker
#         run: docker info

#       - name: Pull base image
#         run: docker pull postgres:15

#       - name: Run integration tests
#         run: |
#           pytest tests -m "integration" \
#             -n auto \
#             --dist=loadscope \
#             --tb=short

#   # -------------------------
#   # MIGRATIONS CHECK
#   # -------------------------
#   check-migrations:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}

#       - name: Install minimal deps
#         run: |
#           pip install alembic sqlalchemy sqlmodel pydantic-settings python-dotenv starlette asyncpg

#       - name: Check duplicate revision IDs
#         run: |
#           DUPES=$(grep -rh '^revision' alembic/versions/*.py \
#             | sed "s/.*= *['\"]//;s/['\"].*//" \
#             | sort | uniq -d)

#           if [ -n "$DUPES" ]; then
#             echo "::error::Duplicate Alembic revision IDs: $DUPES"
#             exit 1
#           fi

#       - name: Check single head
#         env:
#           JWT_SECRET: test
#         run: |
#           python -c "
#           import sys
#           sys.path.insert(0, 'src')
#           from alembic.config import Config
#           from alembic.script import ScriptDirectory

#           cfg = Config('alembic.ini')
#           script = ScriptDirectory.from_config(cfg)
#           heads = script.get_heads()

#           if len(heads) > 1:
#               print(f'::error::Multiple Alembic heads detected: {heads}')
#               sys.exit(1)

#           print(f'Single head: {heads[0]}')
#           "
