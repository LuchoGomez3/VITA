import 'package:flutter/material.dart';

/// Paso 1 · Identificación. Maquetado en el commit siguiente.
class EstablishmentRegisterIdentificationStep extends StatelessWidget {
  /// Crea el paso de identificación del establecimiento.
  const EstablishmentRegisterIdentificationStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Paso 1 · Identificación (WIP)'));
  }
}
