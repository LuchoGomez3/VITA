import 'package:flutter/material.dart';

/// Paso 2 · RENSPA y titular. Maquetado en el commit siguiente.
class EstablishmentRegisterRenspaStep extends StatelessWidget {
  /// Crea el paso de RENSPA y titular del establecimiento.
  const EstablishmentRegisterRenspaStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Paso 2 · RENSPA y titular (WIP)'));
  }
}
